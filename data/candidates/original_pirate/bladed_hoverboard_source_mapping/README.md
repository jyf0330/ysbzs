# Bladed Hoverboard source mapping candidate

This isolated candidate is generated only after verifying the SHA-locked
installed `GameData.db`. It locks the Silver/Gold/Diamond base inheritance,
Abilities `0/1`, the empty base Aura directory, and Toxic Ability `e1` plus
Aura `e2`. The requested Rank 2 use is Gold Toxic, but that rank is an input
selection rather than source-backed popularity evidence.

Both base Abilities and Toxic `e1` declare `Medium`. Directory order does not
prove runtime priority or same-tier execution order. Null `ReferenceValue`
binding, nested timing, Poison application, and complete-build RNG consumption
remain unknown. This candidate is not executable formal content and keeps
`originalRulesAccepted` false.
