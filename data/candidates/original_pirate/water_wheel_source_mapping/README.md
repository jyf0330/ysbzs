# Water Wheel source mapping candidate

Generated only by `tools/add_original_pirate_water_wheel_source_mapping_candidate.py` from the SHA-locked installed `GameData.db` and the isolated candidate workbook.

It locks tier inheritance and both source abilities, including High `CardFired` Haste and Medium adjacent `ItemUsed` self-Charge. It does not establish Haste reapplication semantics, simultaneous-ready tie-breaking, complete initial state, a complete Run, or top-three acceptance.
