# 当前上下文 · 元素背包史

## 当前阶段

- 阶段：Demo v0.1 文档治理与协作规则收敛。
- 当前规则源头：`docs/00_AI_PROJECT_RULES.md`。
- 当前任务入口：`tasks/doing/当前任务.md`。
- 主程序：`index.html`，保持单文件架构。
- 自动化基线：`node test.js`，2026-05-25 验证为 156/156。

## 当前协作方式

- 总控 AI 先判断任务类型并生成任务卡。
- 执行 AI 只读任务卡指定文档和代码，不默认全量读取 `docs/`。
- 验收 AI 只读任务卡、验收清单、测试结果和必要截图/日志。
- 一个任务只允许一个 AI 修改同一代码文件。

## 当前文档状态

- 根目录历史草稿已归档到 `docs/99_归档/`。
- `docs/99_归档/` 只作历史参考，不是当前规则源头。
- 正式 UI 规则采用三层结构：美术风格、HUD 显示层、用户界面与操作规范。
- `核心层与展示层分离设计.md`、`数据文件归档与分类规范.md`、`游戏闭环成熟度评分.md` 为按需补充文档，不再卡住每次 Demo 阶段大需求。

## 近期注意

- 若改 UI / HUD / 菜单 / 信息层级，同步 `HUD与界面信息设计.md`。
- 若改输入、按钮、模式切换或用户反馈，同步 `用户界面与操作规范.md`。
- 若改核心战斗规则，同步 `战斗系统.md`、`技术架构总览.md` 和测试。
- 任意有效改动都同步 `docs/10_CHANGELOG.md`。
