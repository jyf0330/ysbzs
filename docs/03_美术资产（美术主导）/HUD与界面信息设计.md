# HUD 与界面信息设计 · 元素背包史

## 文档状态

- 状态：草稿（Demo v0.1）+ **主界面美术成品化 Goal（2026-06-01 追加，见 §0）**
- 最后更新：2026-06-01
- 参考来源：`index.html` CSS + DOM 结构 + render 函数族逐项梳理

---

## 0、当前主界面美术成品化口径（2026-06-01）

当前 `index.html` 的主界面采用固定 1280×720 战术桌布局，并由运行时缩放适配浏览器窗口。

```
┌────────────────────────────────────────────────────────────────────┐
│ 顶部卷轴状态栏：标题 / 阶段 / 天数 / 波次 / 金币 / 双城堡 / 商店     │
├──────────────┬──────────────────────────────────────┬──────────────┤
│ 左侧调试书记板 │ 中央战术棋盘舞台（#board-stage + #board） │ 右侧符文行动卡 │
│ #debug-dock  │ 8×8 当前运行棋盘 + 坐标轴 + 棋子牌面       │ #asl/.as-card │
├──────────────┴──────────────────────────────────────┴──────────────┤
│ 底部战报条：战斗日志 + 玩家回合状态 + 主操作“开始战斗”              │
└────────────────────────────────────────────────────────────────────┘
```

- 中央棋盘是首屏第一视觉焦点；左右栏不得比棋盘更抢眼。
- 左侧当前不放正式单位卡，开发期固定为 `Debug Dock / 调试书记板`，但视觉降权为只读侧栏。
- 右侧行动槽以“符文行动卡”呈现，而不是表单列表；每张卡包含元素、层数、形状、方向与执行按钮。
- 顶栏和底栏采用卷轴/战报条语义；按钮采用压印暗棕主操作风格。
- 当前运行棋盘为 8×8。历史 13×13 规格仍可能存在于旧测试/旧文档中，未统一前不得把旧规格当作当前 UI 事实。

### 0.1 当前可读性基线（2026-06-01）

本基线服务于 1280×720 固定画布经运行时等比缩放后的桌面首屏体验。后续美术迭代不得为了装饰牺牲以下阅读/点击底线：

| 区域 | 当前基线 |
|---|---|
| 顶栏状态 chip | 阶段/天数/波次/小回合不低于 13px；金币不低于 14px；文本不得横向溢出 |
| 右侧行动卡 | 符文主文字不低于 15px；层数/描述不低于 12px；元素名使用截断而不是挤压 |
| 方向按钮 | CSS 尺寸不低于 22×22px；缩放后当前桌面截图约 29×29px |
| 执行按钮 | 作为行动卡主操作，保持纵向按钮形态；缩放后当前截图约 42×60px |
| 底部主按钮 | 实际截图高度不低于 44px；当前约 50px |
| 左侧 Debug Dock | 开发期可读，当前 10px / 1.55 行高；信息层级低于行动卡和主按钮 |
| 棋盘提示 | 不低于 11px；必须完整露出，不得被底部战报条遮挡 |
| AI 战斗按钮 | 右侧行动槽顶端固定宽度，启用/禁用/推演中都必须能读出“AI战斗”；推演中可有轻量脉冲，但不得遮挡棋盘 |

### 0.2 AI 战斗入口（2026-06-01）

- 右侧行动槽顶端按钮语义为 `AI 战斗`，不是普通后台式“自动编排”。
- 点击后日志顺序应能读出：AI 计划 → AI 战斗执行 → 结算 / 怪物回合。
- 按钮执行中允许短促动效和禁用态；禁用态仍需保留文案可读性，不能只剩图标。
- AI 战斗只辅助当前回合决策，不替代玩家理解战场；棋盘、行动槽已使用状态和战斗日志仍是主要反馈。

---

## 一、整体页面布局

```
┌────────────────────────────────────────────────────┐
│  ⚡ 元素背包史 · Demo v0.1        （h1 标题栏）    │
├──────────────────────────┬─────────────────────────┤
│                          │  [回合信息 box]          │
│   棋盘 #board            │  [英雄状态 box]          │
│   13×13 格子 · 52px/格  │  [行动点 box]            │
│                          │  [战斗日志 box]          │
│   操作提示（底部小字）   │                          │
└──────────────────────────┴─────────────────────────┘
                     │ 覆盖层（条件显示）
          ┌──────────┬───────────┬───────────┐
          │  商店    │ 消息弹窗  │ 行动点分配│
          │  #so     │  #msg     │ #ap-modal │
          └──────────┴───────────┴───────────┘
```

- `#gw`：flex 横排，gap 12px，左棋盘右面板。
- 右面板 `#rp`：宽 290px，flex 竖排，各 `.box` 间距 8px。

---

## 二、棋盘区（#board）

### 基础规格

| 属性 | 值 |
|---|---|
| 格子数 | 13 × 13 = 169 格 |
| 格子尺寸 | 52 × 52 px |
| 格子间隔 | 1px |
| 棋盘背景 | `#1a1a1a` |
| 棋盘边框 | 2px solid `#333` |
| 普通格背景 | `#111827` |

### 格子 CSS 状态类

| 类名 | 含义 | 视觉 |
|---|---|---|
| `.hp` | 英雄占据格 | `#1a0d0d` |
| `.ap` | 行动点攻击范围预览 | `#162608` + 1px `#3a6018` 描边 |
| `.ep` | 元素引爆预览格 | `#2a0a2a` + 1px `#6a206a` 描边 |
| `.ec` | 选中元素格（查看引爆）| 2px `#f0a500` 描边 |
| `.mw-atk` | 怪物下回合攻击预警 | `#3d0000` + 2px `#ef4444` 描边 |
| `.mw-mov` | 怪物下回合移动预警 | `#1a1000` + 1px `#f59e0b` 描边 |

### 格内覆盖层

| 元素 | 类名 | 说明 |
|---|---|---|
| 攻击预览半透明填色 | `.hp-ovl` | position:absolute;inset:0，pointer-events:none |
| 怪物受击伤害数字 | `.dmg-badge` | 顶部居中，红色 `#ff6060`，9px（仅非怪物格显示） |
| 元素预览层数标签 | `.el-prev-badge` | 底部居中，各元素各色，格式 🔥cur→next 或 🔥+N |
| 引爆警告标签 | `.explode-warn` | 底部居中，橙色 `#f59e0b`，7px，优先级高于元素标签 |
| 怪物行动步骤标签 | `.mon-step-badge` | 右上角，橙色 `#f59e0b`，7px，显示 M1/M2/M3 |
| 怪物停留格 | `.mw-final` | `#1a1300` + 2px `#fde68a` 描边 |

### 单位卡片规格

**英雄卡**（`.hm`）

| 属性 | 值 |
|---|---|
| 尺寸 | 48 × 48px，border-radius:6px |
| 英雄 A 颜色 | `#1565C0`（蓝），box-shadow `#1565C0` |
| 英雄 B 颜色 | `#2E7D32`（绿），box-shadow `#2E7D32` |
| 选中状态 | box-shadow `#f0a500`（金色光晕） |
| `.hm-row` 顶行 | flex 横排：`.hm-letter`（14px，A/B） + `.hm-hp`（7px，❤HP） |
| `.hm-inc` 底行 | 7px，紫色 `#c084fc`，预测承伤 ⚠-N（无承伤时隐藏） |

**怪物卡**（`.mm`）四区布局

| 属性 | 值 |
|---|---|
| 尺寸 | 48 × 48px，border-radius:4px |
| 背景 | `#7f1d1d`，边框 1px `#ef4444` |
| 文字颜色 | `#fca5a5` |
| `.mm-top` 顶行 | flex 横排：`.mm-hp`（7px，❤HP）左 + `.mm-atk`（7px，⚔ATK）右 |
| `.mm-icon` 中部 | 13px，👾 图标 |
| `.mm-bot` 底部 | 6px，预测伤害（红色 -N）+ 爆炸警告（橙色 💥） |

**元素格标记**（`.em`）

| 属性 | 值 |
|---|---|
| 尺寸 | 48 × 48px，border-radius:3px |
| 颜色 | 由元素类型决定（见下方元素色表） |
| 内容 | 元素符号 + 层数，12px 加粗 |

---

## 三、右侧信息面板（#rp）

### 3.1 回合信息 box

| DOM ID | 内容 | 样式 |
|---|---|---|
| `#ph` | 当前回合阶段文字 | 20px 加粗，金色 `#f0a500`，居中 |
| `#rc` | 第N波 · 第X/5小回合 | 11px，灰色 `#888`，居中 |
| `#gi` | 💰 N 金币 | 12px，右对齐 |
| `#exa` | ⚡ 一键执行 按钮 | 蓝色 `#1565C0`，与 `#etb` 同风格，13px |
| `#etb` | 结束回合 → 按钮 | 蓝色 `#1565C0`，100%宽，13px |

**回合阶段对应文字：**

| phase | 显示文字 |
|---|---|
| PLAYER | ⚔️ 玩家回合 |
| MONSTER | 👾 怪物行动中… |
| SHOP | 🛒 商店 |
| OVER | 💀 游戏结束 |

### 3.2 英雄状态 box（#hs）

每位英雄一行（`.hi` flex 行）：

- 左侧：英雄名（英雄A蓝色 `#1565C0` / 英雄B绿色 `#2E7D32`，13px 加粗）+ 坐标（10px 灰色）
- 右侧：HP 数值（12px）+ HP 条（宽 90px，高 5px，圆角 3px）

**HP 条颜色规则：**

| HP 占比 | HP 条颜色 |
|---|---|
| > 50% | 绿色 `#22c55e` |
| 25%~50% | 黄色 `#eab308` |
| < 25% | 红色 `#ef4444` |

### 3.3 行动点 box（#asl）

行动槽以 **2 列 `.asl-grid`** 排列，每个迷你卡（`.as-mini`）紧凑显示：

| 区域 | 内容 |
|---|---|
| `.asm-head` | 圆形编号徽章（元素色背景）+ 英雄标签（A-1/B-1…）+ 已使用✓ |
| `.asm-name` | 元素名·槽号·×倍率（如 火·5号·×2），元素色显示 |
| `.asm-dir-row` | 4个方向按钮横排 ↑ ↓ ← →，每个 20×16px |
| 底部 flex 行 | 英雄选择下拉框（flex:1）+ ⚔/✓ 使用按钮（auto宽） |

**行动槽状态样式：**

| 状态 | 类名 | 视觉 |
|---|---|---|
| 普通 | `.as-mini` | `#0d1f3c`，1px `#1e3a5f` |
| 选中 | `.as-mini.sel` | `#1a2f50`，`#f0a500` 描边 |
| 已使用 | `.as-mini.used` | opacity 0.45 |

**方向按钮（`.asd`）：**

| 状态 | 视觉 |
|---|---|
| 普通 | `#0d1f3c`，1px `#1e3a5f`，灰色文字 |
| 激活 | `#f0a500` 背景，黑色文字，`#f0c040` 描边 |

### 3.4 战斗日志 box（#log）

- 固定高度 110px，overflow-y:auto，10px，行高 1.6
- 文字颜色：`#9ca3af`（灰白）
- 每条日志下方有 1px `#1a1a1a` 分割线
- 最新条目在底部（scroll 到底）

---

## 四、覆盖层 / 模态层

### 4.1 商店（#so）

- 触发时机：波次结算后，玩家点"结束回合"进入商店
- 样式：fixed 全屏遮罩，背景 rgba(0,0,0,0.9)，z-index:100
- 内容容器 `#sc`：max-width 680px，居中，金色 2px 描边，圆角 12px

| 区域 | 说明 |
|---|---|
| `.stt` | 🛒 商店 标题，22px 金色居中 |
| `.gd` | 💰 金币数，右对齐，金色加粗 |
| `#scat` | 分类商品列表（攻击方块 / 道具 / 元素强化 / 背包操作） |
| `.csb` | ✅ 完成购买按钮，绿色 `#15803d`，100%宽 |

**商品条目（`.si`）：**

- flex 横排：名称 + 形状预览 | 价格（金色） | 购买按钮
- 已购按钮变蓝（`.bb.sl`），钱不够变灰

### 4.2 元素 Tooltip（#tt）

- 触发：点击棋盘元素格
- 位置：fixed，跟随点击位置
- 内容：各元素叠层数 + willExplode 状态 + 引爆按钮（手动触发 settleExplosions）
- 样式：金色 2px 描边，`#161b22` 背景，z-index:150

### 4.3 消息弹窗（#msg）

- 触发：胜利/失败/提示类消息
- 位置：fixed 屏幕中央，transform:translate(-50%,-50%)
- 样式：金色描边，z-index:200
- 按钮：金色背景，点击关闭

### 4.4 行动点分配弹窗（#ap-modal）

- 触发：从背包装备行动方块时，需指定分配给哪个行动槽
- 位置：fixed 屏幕中央，z-index:300
- 内容：文字提示 + 动态生成槽按钮列表（`.apb`）+ 取消按钮
- 样式：蓝色 2px 描边，min-width 220px

---

## 五、元素色表

| 元素 | 前景色（EC） | 背景色（EB） | 文字 |
|---|---|---|---|
| 火 fire | `#fb923c` | `#431407` | 火 |
| 水 water | `#38bdf8` | `#0c2340` | 水 |
| 风 wind | `#86efac` | `#052e16` | 风 |
| 土 earth | `#d97706` | `#2d1a00` | 土 |

---

## 六、操作提示文案（棋盘下方）

固定一行，10px 灰色 `#555`，文本居中：

> 点击英雄→选中 | 再点空格→移动 | 选行动点后可调方向→点"使用"攻击 | 点元素格→查看引爆

---

## 七、暂不处理 / 未来规划

- 移动端 / 触屏适配
- 攻击范围形状预览动画
- 元素引爆特效动画
- 音效 / 震动反馈
- 多语言文案
- HP 条动画过渡（当前已有 transition:width 0.3s）

---

## 八、召唤引擎 HUD（S1 · 水+召唤垂直切片）

> 关联：[`召唤引擎系统GDD.md`](../01_游戏设计（策划主导）/召唤引擎系统GDD.md) §8。
> 代码：棋盘召唤物卡 `.sm`、引擎统计 `#es`、召唤物受击预警。

### 8.1 召唤物卡片（`.sm`）

棋盘格内召唤物实体，与英雄卡 `.hm` / 怪物卡 `.mm` 同级（占据 `.hp` 格）。

| 属性 | 值 |
|---|---|
| 尺寸 | 92% × 92%，border-radius:6px |
| 背景 | `rgba(14,165,233,0.25)`（水蓝半透明） |
| 边框 | 2px solid `#38bdf8` |
| 文字基色 | `#bae6fd`，9px，竖排居中 |

**卡内三行（上→下）：**

| 区域 | 类名 | 内容 | 样式 |
|---|---|---|---|
| 攻击力 | `.sm-atk` | ⚔{atk}（治疗成长后实时值） | `#fbbf24` 加粗 10px |
| 图标 | `.sm-icon` | 💧 | 14px |
| 生命 | `.sm-hp` | {hp}/{maxHp} | `#7dd3fc` 8px |
| 受击预警 | `.sm-hp`（追加行） | ⚠{合计受击} | `#fda0a0`（仅怪物本回合将攻击该召唤物时显示） |

> `title` 悬浮：`{name} HP:{hp}/{maxHp} ATK:{atk}`。

### 8.2 引擎统计（`#es`）

挂在回合信息 box（§3.1）内，`#gi` 金币行下方。

| 属性 | 值 |
|---|---|
| DOM | `<div id="es">` |
| 样式 | 10px，青色 `#67e8f9`，`margin-top:2px` |
| 文案 | `🌀 场上{summonAlive} · 累计召{summonCount} · 治疗{healCount}`；有成长阶位时追加 `· 成长 召T{n}/疗T{n}/连T{n}` |
| 显隐 | 三值全为 0 时 `display:none`，出现召唤物/有累计后显示 |

数据源：`buildTurnVM().engineStats`（读 `G.engineStats` + 存活召唤物计数）。

### 8.3 召唤物相关棋盘预警

| 表现 | 触发 | 来源 |
|---|---|---|
| 召唤物格受击 ⚠N | 怪物本回合将攻击该召唤物 | `summonIncomingDmg`（`computeMonsterActionPreview`） |
| 死亡留水层 | 召唤物被击杀 → 原地水层 | 走既有元素层显示（§二 `.el-prev-badge` / `.em`） |

### 8.4 S2 成长与连锁（已实现 · 2026-05-30）

> 见 [`成长与连锁反馈系统GDD.md`](../01_游戏设计（策划主导）/成长与连锁反馈系统GDD.md)。

- `#es` 成长阶位：`成长 召T{n}/疗T{n}/连T{n}`（任一项 >0 时显示）。
- 结算 `glog` 连锁摘要：「连锁 ×N！克制 ×N！合计 −N」。
- 完美回合 `glog`：「🌟 完美回合！连锁清场 +3 金」。

**待补（表现层 P2）**：连锁飘字动画、慢镜、阶位提升专用飘字。
