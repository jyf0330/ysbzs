# 12 · 已知问题表（v1.0）

> 来源：代码分析  
> 类型：P0=阻止性 / P1=严重 / P2=一般 / P3=建议  
> 用途：bug/风险/暂缓内容，持续维护

## 一、Bug（实际运行问题）

| # | 类型 | 描述 | 位置 | 影响 | 状态 |
|---|------|------|------|------|------|
| B01 | P2 | `pebble_guard` 和 `bubble_sprite` 在 data.js 中同时存在，中文名都叫"泡泡灵"，但 pebble_guard 有 castleReduce 被动、bubble_sprite 无被动。商店 D1 池同时出现时玩家困惑 | data.js:135-148,297-310 | 玩家体验 | open |
| B02 | P2 | water_droplet Lv3 slot2 使用 sn6（2×2方块），但 sn6 命中格在 8×8 棋盘边缘可能越界（起始列>6 时） | data.js:74, actions.js:22 | 命中格可能少于预期 | open |
| B03 | P2 | `breeze_sprite` 的 grade 是 "白银" 但 tier=1，`calcUnitPrice` 根据 grade 算价格=4，但 tier=1 的同级英雄价格多在 2-3。定位矛盾 | data.js:409-424 | 价格与强度不符预期 | open |
| B04 | P2 | `balance` 英雄 element='fire' 但实际槽中有 water 和 wind 元素，data.js 中 element 字段与实际混合元素槽不一致 | data.js:108 | UI 上显示为火但槽有水风 | open |
| B05 | P2 | `wind_breeze` 青铜阶三个槽全部用 sn12（十字），十字的最小格 `(-1,1)` 在 8×8 棋盘上若英雄在(0,7)时可能越界 | data.js:81 | 命中格不完整 | open |
| B06 | P3 | `setRngSeed(null)` 后 `_rngState = 0`，但 `rngNext()` 走 Math.random 路径。不过后续 `getRngState()` 返回 0 可能被误用 | rng.js:15-16 | 语义模糊 | open |

## 二、风险（可能引发连锁问题）

| # | 类型 | 描述 | 位置 | 影响 | 状态 |
|---|------|------|------|------|------|
| R01 | P1 | **元素结算逻辑在 battle.js 和 ui.js 的 preview 中存在重复**：`settleExplosions()` 和 `buildPreviewGrid()` 各自实现一套伤害计算。如果改公式两处不同步，预览和实战不一致 | battle.js:42-121, ui.js:276-481 | 核心风险 | open |
| R02 | P1 | **shop.js `closeShop()` 结束时调用 `spawnWaveForDay()` 依赖全局 G 状态，但单元测试中未验证完整 10 天商店-战斗闭环** | shop.js:138-168 | 断裂风险 | open |
| R03 | P2 | `syncUnitsToHeroes()` 强制最多 2 个英雄上阵（`if(ui >= 2) return`），但 `MAX_ACTIVE = 6` 无法实际生效 | game.js:73-78 | 扩展性风险 | open |
| R04 | P2 | `recomputeCorePreview()` 在每次 refreshUI 时调用，性能敏感。当前 8×8 棋盘下尚可，但内容扩增后可能卡 | ui.js:595-680 | 性能风险 | open |
| R05 | P2 | 商店 `genShop()` 中 `pool[(i + G.nextUnitId) % pool.length]` 用 `nextUnitId` 做确定性取模，但 `nextUnitId` 还用于创建单位实例 ID | shop.js:67 | 商品随机性耦合 | open |

## 三、待实现（已定义但未实装）

| # | 类型 | 描述 | 位置 | 优先级 |
|---|------|------|------|:------:|
| I01 | P2 | `day_round_config` 目前已配置 `morning` 和 `afternoon`，但 waves.js `spawnWaveForDay` 中 dayHalf=0 对应 morning、dayHalf=2 对应 afternoon。中间 dayHalf=1 在 closeShop 后未统一校验 | shop.js:142-166 | medium |
| I02 | P2 | 怪物能力 `iron_wall` / `block_path` / `target_castle` / `guard_aura` 在 data.js 中定义为 status:'pending'，`runMonsterAbilityHook` 中未实现 | battle.js:390-422, data.js:441-445 | medium |
| I03 | P2 | `MONSTER_TYPES` 中 boss5（炎甲兽）配置了 iron_wall 但 battle.js 未真正实现伤害减免 | data.js:443 | medium |
| I04 | P3 | REWARD_NODE_CONFIG 已定义但未在游戏流程中被实际引用 | data.js:535-544 | low |
| I05 | ✅已修复 | genShop_old() 已删除（G14 清理） | ui.js | low |
| I06 | P3 | `shopFrozen` 用 `Set` 存储，但 `syncUnitsToHeroes` 后商品 id 不变，逻辑正确但测试覆盖不足 | shop.js:56 | low |
| I07 | ✅已修复 | game.js fallback 从写死 pal_005/pal_009 改为动态读取 hero_config/shop_source | game.js:74-85 | low |
| I08 | ✅已修复 | ui.js/preview.js 硬编码 `[0,1,2,4,8]` 倍率数组删除，统一使用 getTierMult() | ui.js, preview.js | low |

## 四、暂缓内容（P3-，不阻碍 S1）

| # | 描述 | 原因 |
|---|------|------|
| S01 | 水元素专属治疗机制（水层→回血） | 当前水的元素层效果≈火，可通过英雄技能实现治疗 |
| S02 | 风元素推拉/减速效果 | 无独立元素机制，英雄被动尚未规划 |
| S03 | 土元素护盾/减伤效果 | 只有阻挡未做防御 |
| S04 | 多元素混合反应 | 只有互克引爆 |
| S05 | 商店消耗品（背包物品） | consumable 函数存在但商品池始终为 0 |
| S06 | 控制台 `toggleFullscreen` F11 | 网页全屏已实现但 Edge/Chrome 下需要实验 |
| S07 | external-data JSON 表 | 只用 basic fallback，未实际连接 data |
| S08 | 10 天完整剧情/事件 | 只有天数推进和波次配置 |

## 五、质量建议

| # | 描述 | 位置 |
|---|------|------|
| Q01 | `ui.js buildRunEndVM()` 和 `showRunEnd()` 在 `switch` 合并后注释遗留 | ui.js:1894-1927 |
| Q02 | `test.js` 中 `global.setTimeout = fn => fn()` 导致异步测试无法真实延迟 | test.js:26 |
| Q03 | `game.js runAiBattleTurn_async` 中 `try/catch` 过于宽泛 | game.js:183-211 |
| Q04 | 所有模块用 `var` 替代 `const/let` 因 Node eval 共享作用域，建议逐步迁移 | test.js:41 |

> 当前状态：P0=0, P1=2, P2=8, P3=5, 暂缓=8  
> 下次更新：持续维护

## 六、变更记录

| change_id | applied_at | 改动 | 覆盖文件 | 测试结果 |
|-----------|-----------|------|---------|---------|
| G14-clean-2026-06-04-19:30 | 2026-06-04 19:30 | 删除 ui.js genShop_old() 死代码；ui.js/preview.js `[0,1,2,4,8]` 硬编码倍率 → getTierMult()；game.js fallback 从写死 fire_starter/water_droplet → 动态读取 hero_config/pal_units；game.js fallback 从写死 pal_005/pal_009 → 动态读取 shop_source/UNIT_DEFS；gpt_test 补 S38-S40 防回流扫描；gpt_test W44 正式通过 | ui.js, preview.js, game.js, gpt_test.js, 12_已知问题表.md | test.js 470/470 ✅; gpt_test.js 306/296/0 ✅ |
