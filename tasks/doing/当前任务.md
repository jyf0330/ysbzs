# 当前任务：AI 游戏评测台方案

## 类型

大需求 / 新模块设计。当前阶段只写 Superpowers spec，不改运行代码。

## 背景

用户确认目标不是“给 ysbzs 写更多测试”，而是抽象一套通用 AI 游戏测试框架/评测台：游戏适配器输入状态，AI/脚本产生动作，游戏执行，记录输出，评估器判定。

## 允许读取

- `docs/00_CURRENT_CONTEXT.md`
- `test.js`
- `playable_day1.js`
- `playable_run.js`
- `replay.js`
- `e2e/smoke.js`
- `benchmarks/ysbzs/README.md`
- `benchmarks/ysbzs/run-benchmark.mjs`
- `index.html` 中 replay、dispatch、action 相关片段

## 允许修改

- `docs/superpowers/specs/2026-06-01-ai-game-evaluation-harness-design.md`
- `tasks/doing/当前任务.md`
- `docs/10_CHANGELOG.md`

## 禁止范围

- 不改 `index.html`
- 不改 `test.js`
- 不改 `playable_day1.js`
- 不改 `playable_run.js`
- 不改 `benchmarks/ysbzs/*`
- 不提交、不暂存当前工作区未归属改动

## Superpowers 流程

- 已使用 `brainstorming`：项目上下文审计、方案比较、设计确认。
- 下一步：用户 review spec 后，再使用 `writing-plans` 写实施计划。

## 验证方式

- 文档自检：无 TODO/TBD、无范围冲突、与现有验证入口职责不冲突。
- 工作区检查：仅报告本轮文档变更，不接管未归属业务改动。
