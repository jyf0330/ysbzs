# 当前任务卡

## 任务类型

工作流规则变更 / 文档治理

## 任务目标

采纳两份 AI 建议中能提升稳定性的部分：清理入口文件、归档历史草稿、拆分根目录数据、建立任务卡分流和最小读取规则，并同步到项目规则与 ywh 系统规则。

补充固定口令 `同步 ywh 工作流` 的明确含义，避免执行 AI 把它误解为业务代码重构或全项目扫描。

补充 `graphify` 的使用边界：只允许总控 AI 用它做项目地图和影响范围分析，不能作为执行 AI 全量读取项目或跳过源码阅读的理由。

## 必须读取

- `docs/00_AI_PROJECT_RULES.md`
- `docs/00_CURRENT_CONTEXT.md`
- `docs/09_DECISIONS.md`
- `docs/10_CHANGELOG.md`
- `docs/04_测试验收（测试主导）/版本发布验收清单.md`
- `C:/Users/11277/.codex/skills/ywh/SKILL.md`
- `C:/Users/11277/.codex/skills/ywh-game/SKILL.md`

## 禁止读取

- 不默认全量读取 `docs/`
- 不把 `docs/99_归档/` 当成当前规则源头

## 允许修改

- 项目 AI 规则和入口文件
- 文档治理相关正式文档
- `docs/99_归档/`
- `tasks/doing/当前任务.md`
- `C:/Users/11277/.codex/skills/ywh/**`
- `C:/Users/11277/.codex/skills/ywh-game/**`

## 禁止修改

- `index.html`
- `test.js`
- 游戏核心规则实现
- 与本轮无关的 `.omx/` 运行状态文件

## 验收标准

- 根目录不再保留 `main.txt`、`srd.txt`、`数据.txt`、`验收结果.txt`。
- 入口文件压缩为薄入口并指向 `docs/00_AI_PROJECT_RULES.md`。
- 项目规则包含任务卡分流、最小读取范围、多 AI 写入保护。
- 项目规则和系统上游明确写入 `同步 ywh 工作流` 固定口令。
- 项目规则和系统上游明确写入 `graphify` 使用边界。
- `ywh` 与 `ywh-game` 同步最小读取和任务卡规则。
- `node test.js` 仍通过。
- `docs/10_CHANGELOG.md` 已更新。

## 冲突处理

如果发现其他 AI 已改同一文件，先输出冲突文件、冲突内容、可能影响和可选方案，不自行覆盖。
