# 当前任务卡

## 任务类型

小修改 / 局部补丁：结束回合处添加一键执行英雄动作按钮

## 任务目标

在"结束回合"按钮旁新增"⚡一键执行"按钮，点击后自动执行所有已配置但未使用的英雄行动槽。

## 完成状态

✅ 已完成（2026-05-26）

## 完成内容

1. 新增 `execAllHeroSlots()` 函数，遍历所有 slot 并对已配置未使用的执行 `USE_SLOT`
2. 结束回合按钮旁新增 `#exa` 按钮（`⚡ 一键执行`），仅在存在可执行槽时可用
3. `buildTurnVM()` 新增 `execAllDisabled` 字段
4. `renderTurn()` 同步控制 `#exa` 禁用态
5. 新增 L 组 3 条测试（case_l_001~003），测试 244→247
6. 同步文档：用户界面与操作规范、HUD与界面信息设计、CHANGELOG

## 禁止读取

- 不默认全量读取 `docs/`
- 不把 `docs/99_归档/` 当成当前规则源头
