# 当前任务卡

## 任务

- 标题：第二天界面异常排查与修复（顶部阶段文案）
- 类型：排查 / 修 bug / 验收
- 目标：修复 day2 相关阶段切换后顶部状态栏文案异常，让玩家能明确识别商店阶段与战斗阶段。

## 范围

- 允许读取：`docs/00_CURRENT_CONTEXT.md`、本任务卡、`index.html`、`test.js`、`docs/10_CHANGELOG.md`
- 允许修改：`index.html`、`test.js`、`docs/10_CHANGELOG.md`、本任务卡
- 禁止修改：其他游戏规则与无关文档

## TDD 记录

- RED：`node test.js`（新增用例 `renderTurn 在 SHOP 阶段显示商店文案，不显示小回合` 失败；实际文案为“第2天下午 · 5/5小回合”）
- GREEN：`node test.js | rg "renderTurn 在 SHOP|测试结果|失败详情|❌"`（新增用例转为 ✅）
- 补充修复：对齐“一天两波”机制后，修复既有失败用例 3 项（`finishMonsters`×2、`case_k_010`×1）。
- 全量验证：`node test.js` → `256 通过，0 失败`。
- 备注：本次包含最小 UI 文案修复 + 测试期望对齐，不改核心战斗数值逻辑。
