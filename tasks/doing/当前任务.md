# 当前任务

## 任务信息

- **任务名称**: 单文件拆分模块化重构
- **任务类型**: 大需求 / 架构重构
- **创建时间**: 2026-06-02
- **来源文档**: `docs/plans/2026-06-02_single-file-refactor-split-modules.md`

## Goal Objective

按 `docs/plans/2026-06-02_single-file-refactor-split-modules.md` 完成单文件拆分模块化重构：将 4981 行 index.html 拆分为 data.js + game.js + ui.js + index.html 四个文件，保持功能不变，通过 `node test.js` 全量测试，并按 YWH 游戏项目收尾规则完成交付。

## 任务分类

大需求 / 架构重构

## 必须交付

- data.js（数据层，~600行）
- game.js（核心层，~2800行）
- ui.js（UI层，~1500行）
- index.html（入口壳，~30行）
- test.js 适配多文件加载
- 技术架构总览文档更新
- CHANGELOG 更新

## 本次不做

- 引入构建工具（Vite/Rollup）
- TypeScript 类型安全
- 状态管理封装
- ADR 文档创建

## 必须读取

- `docs/plans/2026-06-02_single-file-refactor-split-modules.md`（本计划）
- `docs/00_CURRENT_CONTEXT.md`
- `docs/02_程序开发（程序主导）/技术架构总览.md`
- `index.html`（当前源码）
- `test.js`（当前测试）

## 允许修改

- `index.html`
- `data.js`（新建）
- `game.js`（新建）
- `ui.js`（新建）
- `test.js`
- `docs/02_程序开发（程序主导）/技术架构总览.md`
- `docs/10_CHANGELOG.md`

## 禁止修改

- `docs/01_游戏设计（策划主导）/*.md`（策划文档）
- `docs/04_测试验收（测试主导）/*.md`（验收文档）
- `docs/08_ROADMAP.md`
- 任何非本次任务授权的文件

## 验收标准

- [ ] `node test.js` 通过率与拆分前一致（405+ 通过，17 失败）
- [ ] 浏览器打开 index.html 页面正常加载
- [ ] 棋盘显示正常，可点击操作
- [ ] 英雄选择、移动、行动槽使用正常
- [ ] 商店开启、购买、刷新正常
- [ ] 回合推进正常
- [ ] 召唤引擎功能正常

## 验证命令

```bash
node test.js
```

## 文档同步

- 更新 `docs/02_程序开发（程序主导）/技术架构总览.md` 项目结构
- 更新 `docs/10_CHANGELOG.md` 记录重构

## Goal 状态规则

- **Complete**: 所有验收标准通过，文档已同步，已提交
- **Blocked**: 测试通过率下降，或浏览器无法正常运行

## 当前状态

**COMPLETE** - 单文件拆分模块化重构完成，422/422 测试通过

### 已完成

- ✅ Task 1: data.js（539行）
- ✅ Task 2: game.js（2219行）
- ✅ Task 3: ui.js（2022行）
- ✅ Task 4: index.html 重构（778行入口壳）
- ✅ Task 5: test.js 适配多文件加载
- ✅ 更新 `docs/02_程序开发（程序主导）/技术架构总览.md`
- ✅ 更新 `docs/10_CHANGELOG.md`

### 文件结构

```text
ysbzs/
├── index.html      (778行)  # 入口壳：CSS + HTML + 3个script标签
├── data.js         (539行)  # 数据层：常量、配置、怪物/英雄定义
├── game.js        (2219行)  # 核心层：状态管理、战斗、商店、回合
├── ui.js          (2022行)  # UI层：ViewModel构建、DOM渲染、事件
├── test.js                   # 测试：支持多文件/单文件双模式
└── index.html.bak           # 原始单文件备份（4981行）
```

### 待提交

所有修改已就绪，等待用户确认提交。

## TDD 清单

本任务为重构任务，不改变功能行为，TDD 豁免。验证方式：`node test.js` 全量测试通过率不变。

## 实施步骤

按 `docs/plans/2026-06-02_single-file-refactor-split-modules.md` 执行：
1. Task 1: 创建 data.js
2. Task 2: 创建 game.js
3. Task 3: 创建 ui.js
4. Task 4: 重构 index.html
5. Task 5: 适配 test.js
6. Task 6: 清理与验证

## RED 命令

```bash
node test.js
```

预期：405+ 通过，17 失败（当前基准）

## GREEN 命令

```bash
node test.js
```

预期：与拆分前一致（405+ 通过，17 失败）
