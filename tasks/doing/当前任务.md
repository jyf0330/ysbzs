# 当前任务：核心架构收敛——召唤/被动系统接入核心循环

## 任务类型
大需求 / 核心重构

## 目标
把召唤系统（petAct / summonOnCell / processSummons / 被动系统）按 Core → Preview → VM → View 四层接入核心循环，消除直接改 G 的泄漏。

## 允许修改
- index.html（petAct、summonOnCell、processSummons、endPlayerTurn、initGame、buildPreviewGrid、render）
- test.js（补充核心接入的回归测试）
- docs/02_程序开发（程序主导）/技术架构总览.md（同步）
- docs/10_CHANGELOG.md（更新）

## 具体步骤
1. petAct 走 dealDmg → 击杀金币/日志统一
2. summonOnCell 走 clearElementAt / syncBoardElementFromElementCells
3. processSummons 接入 endPlayerTurn 循环
4. G.summons 初始化和 buildPreviewGrid 包含召唤物
5. render() 不再内部调 recomputeCorePreview
6. 非 UI 模式 execAllHeroSlots 整理
7. TDD 回归验证
8. 同步文档

## 禁止范围
- 不改游戏玩法数值
- 不改 UI 表现
