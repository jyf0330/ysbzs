# 当前任务卡

## 任务类型

策划 / 方案设计：商店系统策划方案

## 任务目标

输出《商店系统策划方案》，不改代码。优先沿用 SuperAutoTest 已有商店骨架，映射到元素背包史的单位制商店。

## 完成状态

✅ 已完成（2026-05-26）

## 完成内容

1. 阅读参考项目 `store.rs` / `team_shopping.rs` 全部商店接口
2. 输出完整策划方案到 `docs/01_游戏设计（策划主导）/商店系统策划方案.md`
3. 覆盖：核心循环、商品类型、单位设计(8-12个)、合成升级、经济模型、战斗连接、UI 流程、第一版范围、数据结构草案、风险取舍

## 核心决策

- 第一版只做"单位商店 + 合成升级"一条线
- 不做遗物、装备、羁绊、复杂强化品、多商品池
- 单位是行动槽容器，买单位 = 买 3 个行动槽
- 同名合成升级后行动槽增强，不做特殊被动能力
- 通过 buildHeroesFromUnits() 桥接现有战斗逻辑

## 禁止读取

- 不默认全量读取 `docs/`
- 不把 `docs/99_归档/` 当成当前规则源头
